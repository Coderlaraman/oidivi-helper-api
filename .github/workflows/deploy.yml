name: Deploy OiDiVi Helper Backend to Amazon Lightsail with Docker

on:
  push:
    branches:
      - master
      - main
      - docker_work_branch
    paths:
      - 'oidivi-helper-api/**'
  workflow_dispatch:

jobs:
  dry-run:
    runs-on: ubuntu-latest
    steps:
      - name: Echo de prueba
        run: echo "✅ API workflow disparado correctamente desde ${{ github.ref }}"